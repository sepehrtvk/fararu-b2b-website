export interface CenterModel {
  address: string;
  areaRef: number;
  dcCategory: number;
  dcCode: string;
  dcName: string;
  dcguid: string;
  id: number;
  isCentralize: boolean;
  status: number;
}
